<%= form_with(model: tvshow, local: true) do |form| %>
  <% if tvshow.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tvshow.errors.count, "error") %> prohibited this tvshow from being saved:</h2>

      <ul>
      <% tvshow.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>


  <div class="field">
    <%= form.label :time %>
    <%= form.time_select :time %>
  </div>
  <div class="field">
    <%= form.label :favourite %>
    <%= form.check_box :favourite %>
  </div>
  <div class="field">
    <p>Channel</p>
    <%= form.select :channel_id, options_for_select(@channels.collect{|x| [x.name, x.id]}) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
